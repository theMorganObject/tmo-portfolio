import NewProjectDetail from '@/components/gallery/project/NewProjectDetail';

export default function Page() {
  return (
    <section className='min-h-screen bg-blue-12 font-sans pt-10'>
      {/* TODO: implement this new detail component on all projects, remove "New" from component name once finished, and remove all legacy code. */}
      <NewProjectDetail />
    </section>
  );
}
